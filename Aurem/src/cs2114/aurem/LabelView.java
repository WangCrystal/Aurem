package cs2114.aurem;

import android.graphics.Canvas;
import android.content.Context;
import android.util.AttributeSet;

/**
 * // -------------------------------------------------------------------------
/**
 *  This is a view to show the labels for the frequency bands to the
 *  left of the sliders.
 *
 *  @author Joseph O'Connor (jto2e);
 *  @author Laura Avakian (lavakian);
 *  @author Barbara Brown (brownba1);
 *  @version 2012.04.18
 */
public class LabelView extends android.view.View
{

    /**
     * Constructor for LabelView objects.
     * @param context Context the context.
     * @param attrs AttributeSet the attributes.
     */
    public LabelView(Context context, AttributeSet attrs)
    {
        super(context, attrs);
        //Construct.
    }

    /**
     * Called when the view is drawn.
     * @param canvas Canvas the canvas.
     */
    @Override
    public void onDraw(Canvas canvas)
    {
        //.
    }

}
